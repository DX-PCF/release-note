NGINX Open Source および NGINX Plus に、ヒープバッファオーバーフロー（CWE-122）の脆弱性が存在します 
 。
本脆弱性は、 「 map 」 ディレクティブで正規表現マッチングを使用し、文字列式の中で正規表現のキャプチャ変数（$1等）を 「 map 」 の出力変数より前に参照している場合、または特定の条件下でキャッシュ不可能な変数を参照している場合に発生します 
 。
認証されていないリモートの第三者が特別に細工した HTTP リクエストを送信することで、ワーカープロセスのヒープメモリを破損させ、プロセスの強制再起動によるサービス不能（DoS）を引き起こすほか、アドレス空間配置のランダム化（ASLR）が無効化・回避可能な環境では任意のコード実行（RCE）につながる可能性があります 
 。

影響調査
対象のサーバー環境で本脆弱性の影響を受けるか確認する手順は以下の通りです 
 。

NGINX バージョンの確認
サーバー上で以下のコマンドを実行し、稼働中の NGINX のバージョンを確認します 
 。

bash
nginx -v
設定ファイルの確認
NGINX の設定ファイル（ nginx.conf など）内を検索し、 「 map 」 ディレクティブにおいて正規表現マッチングおよびキャプチャ変数を使用している設定が存在するか確認します 
 。特に、文字列式の中でキャプチャ変数を 「 map 」 出力変数よりも前に参照している設定パターンの有無を調査します 
 。

エラーログの検出
NGINX のエラーログ（ error.log ）にワーカープロセスのシグナル 11 による異常終了（ worker process exited on signal 11 ）が頻発していないか確認します 
 。


Keycloak に、認可チェックの欠落（CWE-862）に起因する深刻な権限昇格の脆弱性が存在します。
本脆弱性は、ユーザーのなりすまし機能（ 「 impersonation 」 ロール）に関するアクセス制御の不備によって引き起こされます。
「 impersonation 」 ロールを付与されたユーザーがレルム管理者（Realm Administrator）になりすます際の権限レベル検証が行われないため、攻撃者がレルム管理者権限を奪取し、ユーザー、クライアント、ロールの追加・改ざん・削除を含む全管理権限を取得する可能性があります。

影響調査
自社の Keycloak 環境が影響を受けるか確認する手順は以下の通りです。

バージョン情報の確認
Keycloak の管理コンソール、起動ログ、またはコンテナイメージタグより、動作中の Keycloak バージョンを確認します。

ロール割り当ての緊急監査
管理コンソールより、ユーザーやグループに 「 impersonation 」 ロールが付与されているか監査します。

「 realm-management 」 クライアントの 「 impersonation 」 ロールが一般ユーザーや限定的な権限を持つ管理者に割り当てられていないか点検します。

監査ログの精査
Keycloak のイベントログ（Admin Events）にて、 「 IMPERSONATE 」 イベントの実行履歴を確認し、レルム管理者アカウントに対する不審ななりすまし操作が行われていないかを検証します。

影響調査詳細
Keycloak  への影響についての詳細調査結果は以下の通りです。

Keycloak  への影響判定:
本脆弱性（CVE-2026-17526）は、Keycloak の 「 keycloak-services 」 コンポーネントにおけるなりすまし実行時の対象ユーザー権限チェック不備に起因します。公式の修正パッチは Keycloak 26.4.16 および 26.6.7 にて提供されています。Red Hat の評価基準では「明確に非影響と提示されていない限り、過去の旧バージョンも潜在的に影響を受けるとみなす」とされているため、Keycloak  においても 「 impersonation 」 機能を利用している場合は本脆弱性の 潜在的な影響を受ける可能性があります。

調査に関する補足:
専用の自動分析ツール（Keycloak Analysis）が現在利用不可であるため、公式セキュリティアドバイザリ（RHSA-2026:68276 / RHSA-2026:68277 等）およびソースコードの情報を基に手動で詳細分析を実施しました。

----
----
Keycloak の SAML Redirect Binding 実装（ 「 keycloak-services 」 ）に、ネイティブメモリの解放不備（CWE-401）によるサービス拒否（DoS）の脆弱性が存在します。
本脆弱性は、SAML メッセージの DEFLATE 圧縮および解凍処理を行うカスタムヘルパーが、使用後にネイティブ zlib メモリを正常に解放しないことに起因します。
認証されていないリモートの第三者が不正な構造の SAML リクエストを繰り返し送信することで、Java 仮想マシン（JVM）ヒープ外のネイティブメモリが徐々に枯渇し、Keycloak サービス全体の強制停止や応答不能状態を引き起こす可能性があります。

影響調査
自社の Keycloak 環境における影響の有無を確認する手順は以下の通りです。

SAML 機能の利用状況確認
Keycloak の管理コンソールで、SAML プロトコルを用いたアイデンティティプロバイダ（IdP）連携やクライアント設定が有効になっているか確認します。

ネイティブメモリ使用量の監視
Keycloak プロセスの Resident Set Size（RSS）メモリを監視し、JVM ヒープサイズに変動がないにもかかわらず RSS が増加し続けていないかを検証します。

ログの確認
Keycloak のアクセスログやエラーログにおいて、 「 /realms/*/protocol/saml 」 エンドポイントに対する特定の送信元からの大量リクエストや、zlib 解凍エラーが多発していないか確認します。

影響調査詳細
Keycloak  への影響についての詳細な調査結果は以下の通りです。

Keycloak  への影響判定:
本脆弱性は SAML Redirect Binding の DEFLATE 解凍ロジック全体に起因しており、明確に非対象と指定されていない過去の旧バージョンすべてに潜在的なリスクが存在します。そのため、Keycloak （特に SAML 連携を使用している環境）においても本脆弱性の 影響を受ける可能性が高い と判定されます。

調査に関する補足:
専用の自動分析ツール（Keycloak Analysis）が現在利用不可であるため、公式セキュリティアドバイザリ（RHSA-2026:68276 等）およびオープンソースのリポジトリ変更履歴（GitHub Issue #52839）に基づき手動で分析を実施しました。



