# 1.34
Kubernetes 1.34.7のリリースノートの概要は以下の通りです。このバージョンは2026年4月15日にリリースされ、いくつかのバグ修正と依存関係の更新が含まれています。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **バグ修正** | `kube-proxy`がnftablesの新しいバージョンでクラッシュする問題が修正されました。 |
| **バグ修正** | コンテナ再起動後にデバイスプラグインの受付に失敗する問題が修正されました。 |
| **バグ修正** | 監査ログフィルタにおけるリクエスト遅延の計算が正しく行われるように修正されました。 |
| **改善** | ビルドに使用されるGoのバージョンが1.25.9に更新されました。 |

---
### 詳細

#### 🐞 バグ修正
*   **kube-proxy**: `nftables` v1.1.3以降を使用しているシステムで`kube-proxy`がクラッシュする問題が修正されました。
*   **デバイスプラグイン**: コンテナの再起動後に、デバイスプラグインがPodのアドミッション（受付）に失敗することがある不具合が修正されました。
*   **監査ログ**: APIサーバーの監査ログフィルタで、リクエストの遅延時間が正しく計算されるように修正されました。

#### ⬆️ 依存関係の更新
*   ビルドに使用されるGoのバージョンが1.25.9に更新されました。
*   `spdystream`ライブラリがv0.5.1に更新されました。
*   `go.opentelemetry.io/otel`ライブラリがv1.41.0に更新されました。

このパッチリリースは、主にシステムの安定性向上を目的とした修正が含まれています。

# 1.35

Kubernetes 1.35.1のリリースノートの概要は以下の通りです。このリリースは、バグ修正といくつかの機能改善に焦点を当てています。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **機能改善** | Kubernetesのビルドに使用されるGoのバージョンが1.25.6に更新されました。 |
| **バグ修正** | スケジューラのパフォーマンスリグレッションや、`kubectl exec`でのパニックなど、いくつかの不具合が修正されました。 |
| **バグ修正** | Dynamic Resource Allocation (DRA)において、デバイスが二重に割り当てられる競合状態が修正されました。 |
| **バグ修正** | SELinuxの警告コントローラーが、完了したPodに対してイベントを送信しないように修正されました。 |

---
### 詳細

#### ✨ 機能改善
* Kubernetesのビルドに使用されるGoのバージョンが1.25.6に更新されました。

#### 🐞 バグ修正
* **スケジューラ**: v1.35.0で発生した、APIクライアントのスロットリングによって引き起こされるスケジューラのパフォーマンスリグレッションを修正するため、`SchedulerAsyncAPICalls`機能ゲートが無効化されました。
* **kubectl**: `kubectl exec`コマンド実行時に、ターミナルサイズのデリゲートが初期化されていない場合に発生する可能性があったパニックが修正されました。
* **Dynamic Resource Allocation (DRA)**: 多数のPodが迅速にスケジューリングされる際に、競合状態によって同じデバイスが異なるリソース要求に二重に割り当てられてしまう問題が修正されました。
* **kubelet**: `V().Error()` を使用した一部のデバッグ・情報メッセージが、設定されたログの詳細度に関わらず常に出力されてしまう問題が修正され、ログの冗長性が正しく守られるようになりました。
* **APIサーバー**: v1.29で発生したリグレッションが修正され、`apiserver_watch_events_sizes`メトリックが、出力されるウォッチトラフィックの合計を再び報告するようになりました。
* **Pod**: v1.34で発生した、`$`の後にマルチバイト文字を含む値を持つ環境変数でPodを開始する際のリグレッションが修正されました。



Kubernetes 1.35.2のリリースノートの概要は以下の通りです。このバージョンは、主に利用されているプログラミング言語のバージョンアップを目的とした、小規模なパッチリリースです。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **機能改善** | Kubernetesのビルドに使用されるGoのバージョンが1.25.7に更新されました。 |

---
### 詳細

公式のチェンジログによると、Kubernetes 1.35.2での主な変更点は、ビルドに使用されるGoのバージョンが1.25.7に更新されたことのみです。

このリリースには、その他のバグ修正や新機能の追加、既知の問題に関する記載はありませんでした。

Kubernetes 1.35.3のリリースノートの概要は以下の通りです。このリリースは、2026年3月18日に公開され、いくつかのバグ修正と改善が含まれています。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **バグ修正** | Dynamic Resource Allocation (DRA)のスケジューリングロジックが修正されました。 |
| **改善** | `client-go`のテスト用ライブラリである`fake client-go`において、非推奨となっていた`NewSimpleClientset`が再び利用可能になりました。 |
| **その他** | 不安定なテストケースがいくつか修正され、信頼性が向上しました。 |

---
### 詳細

#### 🐞 バグ修正・改善
* **Dynamic Resource Allocation (DRA)**: スケジューラが、リソースの割り当て情報を処理した後に開始されるように修正され、スケジューリングの競合状態を修正しました。
* **client-go**: テスト目的で使用される`fake`クライアントにおいて、`NewSimpleClientset`関数が非推奨から復帰しました。
* **ビルド**: このバージョンのKubernetesは、Go 1.25.7を使用してビルドされています。

このパッチリリースには、重大な新機能の追加や破壊的変更は含まれていません。


Kubernetes 1.35.3のリリースノートの概要は以下の通りです。このリリースは、2026年3月18日に公開され、いくつかのバグ修正と改善が含まれています。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **バグ修正** | Dynamic Resource Allocation (DRA)のスケジューリングロジックが修正されました。 |
| **改善** | `client-go`のテスト用ライブラリである`fake client-go`において、非推奨となっていた`NewSimpleClientset`が再び利用可能になりました。 |
| **その他** | 不安定なテストケースがいくつか修正され、信頼性が向上しました。 |

---
### 詳細

#### 🐞 バグ修正・改善
* **Dynamic Resource Allocation (DRA)**: スケジューラが、リソースの割り当て情報を処理した後に開始されるように修正され、スケジューリングの競合状態を修正しました。
* **client-go**: テスト目的で使用される`fake`クライアントにおいて、`NewSimpleClientset`関数が非推奨から復帰しました。
* **ビルド**: このバージョンのKubernetesは、Go 1.25.7を使用してビルドされています。

このパッチリリースには、重大な新機能の追加や破壊的変更は含まれていません。

Kubernetes 1.35.4のリリースノートの概要は以下の通りです。このバージョンは2026年4月14日にリリースされ、いくつかの重要なバグ修正が含まれています。

---
### 主な変更点

| カテゴリ | 概要 |
|---|---|
| **バグ修正** | サイドカーコンテナを持つPodで、メインコンテナがクラッシュ後に再起動しない不具合が修正されました。 |
| **バグ修正** | StatefulSetのParallel Pod管理におけるリグレッション（v1.35で発生）が修正されました。 |
| **バグ修正** | APIサーバーの監査ログにおけるリクエスト遅延の注釈に関するリグレッション（v1.34以降で発生）が修正されました。 |
| **改善** | ビルドに使用されるGoのバージョンが1.25.9に更新されました。 |

---
### 詳細

#### 🐞 バグ修正
*   **コンテナの再起動問題**: サイドカーコンテナ（`restartPolicy: Always`が設定されたinitContainer）と`startupProbe`を持つPodで、メインコンテナがクラッシュした後に再起動されず、`RestartCount: 0`のまま停止してしまう不具合が修正されました。
*   **StatefulSetのリグレッション**: v1.35で発生したStatefulSetのParallel Pod管理に関するリグレッションを修正するため、`MaxUnavailableStatefulSet`機能がデフォルトで無効になりました。
*   **APIサーバーの監査ログ**: リクエストの処理に500ミリ秒以上かかった場合に、APIサーバーの監査ログにリクエスト遅延の注釈が報告されないことがあったv1.34以降のリグレッションが修正されました。
*   **kube-proxy**: `nft` バージョン1.1.3を使用しているシステムで、`kube-proxy`のnftablesモードが動作するように修正されました。

このパッチリリースには、新機能の追加は含まれておらず、主に安定性の向上を目的としたバグ修正が行われています。

# 1.36.0
Kubernetes 1.36.0、コードネーム「ハル (Haru)」は、2026年4月22日にリリースされました。このリリースは、新しいAPIの追加よりも、既存機能の安定化、運用性の向上、セキュリティ強化に重点を置いています。合計で70の機能改善が含まれており、そのうち18が安定版（GA）に昇格しました。

---
### ✨ 安定版 (GA) に昇格した主な機能

| カテゴリ | 機能 | 概要 |
|---|---|---|
| **セキュリティ** | **User Namespacesのサポート** | コンテナ内のrootユーザーをホスト上の非特権ユーザーにマッピングし、コンテナからの権限昇格リスクを大幅に軽減します。 |
| **セキュリティ** | **Mutating Admission Policies** | CEL式を使用してリソースの変更を宣言的に定義できるようになり、外部のWebhookサーバーなしでアドミッションコントロールが可能になります。 |
| **ストレージ** | **SELinuxボリュームの高速なラベリング** | ボリューム全体にマウント時にSELinuxラベルを適用することで、Podの起動時間を大幅に短縮します。 |
| **ストレージ** | **OCI VolumeSource (ImageVolume)** | OCIイメージを直接読み取り専用ボリュームとしてマウントできるようになります。 |
| **セキュリティ** | **Kubelet APIのきめ細かな認可** | ノードが他のノードに属するSecretを読み取れなくなり、ノード間の分離を強化します。 |

---
### ⚠️ 重要な非推奨と削除

このリリースには、ユーザーの対応が必要となる可能性のある重要な変更が含まれています。

*   **`gitRepo` ボリュームプラグインの削除**
    v1.11から非推奨だった `gitRepo` ボリュームタイプが完全に削除されました。この機能はセキュリティリスクを抱えていたため、今後は`initContainer`を使用してリポジトリをクローンする方法に移行する必要があります。

*   **Ingress-Nginx プロジェクトの廃止**
    Kubernetesコミュニティによる `ingress-nginx` のサポートが終了しました。既存のバージョンは引き続き機能しますが、今後はセキュリティパッチやアップデートは提供されません。ユーザーは代替のIngressコントローラへの移行を検討することが強く推奨されます。

*   **`service.spec.externalIPs` の非推奨**
    セキュリティ上の懸念から、`externalIPs` フィールドが非推奨となり、使用時に警告が表示されるようになりました。今後はIngressやGateway APIの使用が推奨されます。

---
### 🚀 その他の注目すべき改善

*   **Dynamic Resource Allocation (DRA)**: デバイスのTaints and Tolerationsのサポートなど、ハードウェアリソースの動的な割り当てに関する多くの機能改善がアルファ版・ベータ版として導入され、将来的な拡張が期待されます。
